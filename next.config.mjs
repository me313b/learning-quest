/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // YouTube thumbnails are loaded with a plain <img>, so no remotePatterns are
  // needed here.
};
export default nextConfig;
